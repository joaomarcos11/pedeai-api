db-persistentvolume
db-persistentvolumeclaim
db-configmap
db-deployment
db-service
app-configmap
app-deployment
app-service
app-hpa

minikube addons enable metrics-server

comando para carregar imagem do docker:
	minikube image load <image name>

minikube service hello-minikube1 --url

kubectl run -i --tty load-generator --rm --image=busybox:1.28 --restart=Never -- /bin/sh -c "while sleep 0.01; do wget -q -O- http://pedeai-app:8080/clientes; done"
